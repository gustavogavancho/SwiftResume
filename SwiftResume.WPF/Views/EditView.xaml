<UserControl x:Class="SwiftResume.WPF.Views.EditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SwiftResume.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Content="Return" Command="{Binding ReturnCommand}" Style="{StaticResource PrimaryNormalButtonSytle}" Width="50" Height="30" Margin="5" HorizontalAlignment="Right"/>
        <TextBlock Grid.Row="1" Text="Edit View" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
    
</UserControl>
